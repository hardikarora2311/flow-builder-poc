# A transitive dep of the msw CLI (@inquirer/confirm) declares engines
# node >=22.13; the local toolchain is 22.11. It is a scaffold-only CLI dep
# and does not affect runtime, so engine checks are relaxed for installs.
ignore-engines true
